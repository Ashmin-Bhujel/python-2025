# Default Parameter Value
# TODO: Write a function that greets a user. If no name is provided, it should greet with a default name.
# * NOTE: You should only provide default values to the parameters which are last in parameters list
def greet(name="User"):
    print(f"Hello, {name}!")


greet()
greet("Ashmin")
